val a =3
val s ="Hiii"
println(s)
val mn=
    "abcd"

def max(x: Int, y: Int) = {
    if(x > y)
        x
    else
        y
}

def min(x: Int, y: Int) = if (x < y) x else y
min(4,5)
max(30,60)

def hello() = println("hiii")


val numValues =Array("zero","one","two")
val numValues2=Array.apply("zero","one","two")

def check(a: Int) = {
  if(a == 1)
    true
  else
    false

}
check(2)
